flask
flask-socketio
flask-sqlalchemy
flask-cors
alembic
pillow
gunicorn
eventlet
requests
pyjwt
# psycopg2-binary is only required when deploying with PostgreSQL (DATABASE_URL is set).
# In the Nix production environment, python3Packages.psycopg2 from nixpkgs is used instead.
psycopg2-binary
pytest
# Docs (npm run docs:serve / docs:build) — already provided by shell.nix in the Nix dev shell.
mkdocs
mkdocs-material
